# @connectingmatrix/logger final runtime contracts

This file is generated by the final process-monitor / project / nodes / workflow-AI / Giga-agents hardening pass.

## Ownership

- Logger runtime
- decorator logging
- process monitor registry
- CPU/RAM/PID/internal process tracking
- abort/kill/heartbeat
- live process logs

## Public API contracts

- `Logger.setup({ files, level, sockets })`
- `Logger.info/debug/warn/error(...)`
- `Logger.trackProcess(processId,patch)`
- `Logger.processLog(processId,level,message,data)`
- `Logger.abortProcess(processId,reason)`
- `Logger.processMonitorSnapshot()`
- `processMonitoring.list(filter?)`
- `processMonitoring.live(handler?)`
- `processMonitoring.logs.list(processId?,limit?)`
- `processMonitoring.logs.live(processId,handler?)`
- `processMonitoring.abort(processId,reason)`
- `processMonitoring.kill(processId,reason)`
- `processMonitoring.heartbeat(processId,{status,message,patch})`
- `@Log / LogDecorator`

## Routes / launcher

- `GET /process-monitor/list`
- `GET /process-monitor/live`
- `GET /process-monitor/logs/live`
- `POST /process-monitor/abort`
- `POST /process-monitor/kill`
- `POST /process-monitor/heartbeat`
- `GET /logger/process-monitor`

## Notes

- Processes are normalized into Workflows, Ai Agents, Swarm, Projects, Nodes, Chat, Server, Files, Drive, or Other.

## Stub launcher

Run the local package launcher with:

```bash
npm run build
node playground.mjs
```
